version: 2

build:
  os: ubuntu-20.04
  tools:
    python: "3.7"
  apt_packages:
    - cmake

sphinx:
  configuration: docs/conf.py

submodules:
  include: all

python:
  install:
    - method: setuptools
      path: .